name: pageview_parallax
description: A simple library for easily creating a parallax effect.
version: 1.0.0-alpha.1
homepage: https://www.aspectapps.com
repository: https://github.com/amogh-jrules/pageview_parallax
issue_tracker: https://github.com/amogh-jrules/flutter-pagination/issues

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
